apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  # ClusterIP karena akses hanya melalui Ingress Controller 
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
