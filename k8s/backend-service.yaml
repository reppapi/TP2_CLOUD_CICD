apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  # ClusterIP karena akses hanya melalui Ingress Controller 
  type: ClusterIP
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
